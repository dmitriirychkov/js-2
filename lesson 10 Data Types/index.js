// 1. string
const favoriteDink = 'black tea';
console.log(favoriteDink);

// 2. number
const numberOfMugs = 3;
console.log(numberOfMugs);

// 3. boolean
const isColdDrink =true;
console.log(isColdDrink);

// 4. null
const studentFavoriteDrink = null;
console.log(studentFavoriteDrink);

// 5. undefined
let carOwner = undefined;
console.log(carOwner);

// 6. object
const drink = {
    favoriteDink: 'Tea',
    numberOfMugs: 3,
    isColdDrink: true,
};
console.log(drink);

// 7. symbol
const id = Symbol('id');
console.log(id);

// 8. bigint
const bigIntNumber = BigInt(10);
console.log(bigIntNumber);
